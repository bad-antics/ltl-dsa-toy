// ltl-dsa-toy/main.ltl
//
// Textbook DSA signatures — FIPS 186 parameters.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dsa-toy v0.1.0")
    io::println("Textbook DSA signatures — FIPS 186 parameters.")
    0
}
