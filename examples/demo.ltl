// examples/demo.ltl — runnable demo for ltl_dsa_toy

import std::io
use ltl_dsa_toy as lib

fn main() {
    io::println("ltl_dsa_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}